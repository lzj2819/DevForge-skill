# DevForge v1.4 Foundation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extend XML schemas, add reference documents, and update shared tools to support v1.4 security/observability/data features.

**Architecture:** This plan lays the groundwork for all v1.4 enhancements by extending the three-layer XML schema (System/Module/Component) with new nodes for security posture, observability, and OLAP data models. It also updates shared tool specs and the STATE.md template.

**Tech Stack:** Markdown, YAML frontmatter, XML schema definitions

---

## File Structure

| File | Action | Responsibility |
|------|--------|---------------|
| `references/xml-schemas.md` | Modify | Extend System/Module/Component layer schemas |
| `references/threat-modeling-guide.md` | Create | STRIDE methodology reference for threat-modeling skill |
| `references/observability-architecture.md` | Create | SLO/SLI/alerting patterns reference |
| `references/data-quality-framework.md` | Create | Data quality rules and dimensional modeling reference |
| `skill/tools/precondition-checker.md` | Modify | Add new phases: `threat_modeling_completed`, `data_pipeline_completed` |
| `skill/tools/intervention-checkpoint.md` | Modify | Add new commands: `[THREAT_MODEL]`, `[DATA_PIPELINE]`, `[SLO]`, `[PERFORMANCE_TEST]`, `[FRONTEND_DESIGN]` |
| `skill/tools/artifact-manager.md` | Modify | Add new artifact types: `security.xml`, `THREAT_MODEL_REPORT.md`, `dataflow.xml`, `schema-olap.sql`, `observability/*.yaml` |
| `devforge-state.md` | Modify | Add Characteristic Tags, Loaded Extensions, Security Posture, Data Pipeline Status sections |

---

### Task 1: Extend `references/xml-schemas.md` — Security Nodes

**Files:**
- Modify: `references/xml-schemas.md:96-103` (Security section)

**Context:** Current Security section only defines `Authentication` and `Encryption`. We need to add `Authorization`, `ThreatModel`, `KeyManagement`, and `Audit`.

- [ ] **Step 1: Add `Authorization` node spec**

```markdown
### `Authorization`
Access control policies.

**Authorization attributes**:
- `model` (required): One of {RBAC, ABAC, PBAC}

**Authorization children**:
- `Role` (0+): Attributes:
  - `id` (required): Role identifier
  - `permissions` (required): Comma-separated permission list or "*" for all
- `Policy` (0+): Attributes:
  - `type` (required): One of {resource-based, action-based, context-based}
  - `target` (optional): Resource pattern this policy applies to
```

- [ ] **Step 2: Add `ThreatModel` node spec**

```markdown
### `ThreatModel`
Structured threat analysis using STRIDE methodology.

**ThreatModel children**:
- `STRIDE` (1): Contains `Threat` nodes
  - `Threat` attributes:
    - `category` (required): One of {Spoofing, Tampering, Repudiation, InformationDisclosure, DoS, Elevation}
    - `module` (required): Target module id
    - `mitigation` (required): Mitigation strategy identifier
    - `severity` (optional): One of {Critical, High, Medium, Low}. Default: "Medium"
```

- [ ] **Step 3: Add `KeyManagement` node spec**

```markdown
### `KeyManagement`
Encryption key lifecycle management.

**KeyManagement children**:
- `Strategy` (1): Attributes:
  - `type` (required): One of {HashiCorp-Vault, AWS-KMS, Azure-KeyVault, GCP-KMS}
  - `rotation` (required): Rotation period in days (e.g., "90d")
```

- [ ] **Step 4: Add `Audit` node spec**

```markdown
### `Audit`
Security audit logging configuration.

**Audit children**:
- `LogEvents` (1): Comma-separated list of events to log (e.g., "authn,authz,data-access")
- `Retention` (1): Retention period in days (e.g., "2555d")
```

- [ ] **Step 5: Commit**

```bash
git add references/xml-schemas.md
git commit -m "feat: extend Security nodes in xml-schemas.md"
```

---

### Task 2: Extend `references/xml-schemas.md` — DataModel OLAP Support

**Files:**
- Modify: `references/xml-schemas.md:50-83` (DataModel section)

**Context:** Current DataModel only supports OLTP (ddlType="table/view/collection"). Need to add OLAP-specific types and attributes.

- [ ] **Step 1: Extend `DataModel` attributes**

Add to the DataModel attributes list:
```markdown
- `modelType` (optional): One of {oltp, olap-dimension, olap-fact, olap-aggregate}. Default: "oltp"
- `partitionKey` (optional): Field name for partitioning (OLAP only)
- `clusteringKey` (optional): Comma-separated field names for clustering (OLAP only)
- `retentionDays` (optional): Data retention period in days (OLAP only)
```

- [ ] **Step 2: Extend `Field` attributes for OLAP**

Add to the Field attributes list:
```markdown
- `isPartitionKey` (boolean, optional): Whether this field is the partition key
- `isClusteringKey` (boolean, optional): Whether this field is part of the clustering key
- `isPII` (boolean, optional): Whether this field contains personally identifiable information
```

- [ ] **Step 3: Commit**

```bash
git add references/xml-schemas.md
git commit -m "feat: add OLAP support to DataModel schema"
```

---

### Task 3: Extend `references/xml-schemas.md` — Module Layer Frontend Profile

**Files:**
- Modify: `references/xml-schemas.md:105-168` (Module Architecture section)

**Context:** Add FrontendProfile support for frontend modules.

- [ ] **Step 1: Add `FrontendProfile` node spec under Module layer**

```markdown
### `FrontendProfile`
Frontend-specific configuration (only for modules with `type="frontend"`).

**FrontendProfile children**:
- `Framework` (1): Attributes:
  - `type` (required): One of {react, vue, angular, svelte}
  - `rendering` (required): One of {csr, ssr, ssg}
- `StateManagement` (1): Attributes:
  - `type` (required): One of {redux, zustand, pinia, vuex, context-api}
- `ComponentLibrary` (1): Attributes:
  - `tiered` (boolean, default "false"): Whether components are organized in base/general/business tiers
- `PerformanceTargets` (1): Attributes:
  - `fcp` (optional): First Contentful Paint target (e.g., "1.8s")
  - `lcp` (optional): Largest Contentful Paint target (e.g., "2.5s")
  - `cls` (optional): Cumulative Layout Shift target (e.g., "0.1")
  - `fid` (optional): First Input Delay target (e.g., "100ms")
- `I18n` (1): Attributes:
  - `strategy` (required): One of {static-bundle, dynamic-load}
  - `locales` (required): Comma-separated locale codes
```

- [ ] **Step 2: Commit**

```bash
git add references/xml-schemas.md
git commit -m "feat: add FrontendProfile to Module layer schema"
```

---

### Task 4: Create `references/threat-modeling-guide.md`

**Files:**
- Create: `references/threat-modeling-guide.md`

- [ ] **Step 1: Create the file with STRIDE methodology reference**

```markdown
# Threat Modeling Guide

Reference document for `devforge-threat-modeling` skill.

## STRIDE Categories

| Category | Definition | Example Threat | Typical Mitigation |
|----------|-----------|----------------|-------------------|
| **S**poofing | Impersonating someone or something | Attacker fakes JWT token | MFA, token binding |
| **T**ampering | Modifying data or code | Request payload modified in transit | Digital signatures, TLS |
| **R**epudiation | Denying an action | User denies placing order | Audit logging, non-repudiation |
| **I**nformation Disclosure | Exposing data to unauthorized parties | Database dump exposed | Encryption, access control |
| **D**oS | Denying service to users | API flooded with requests | Rate limiting, DDoS protection |
| **E**levation of Privilege | Gaining unauthorized capabilities | Regular user becomes admin | RBAC, privilege separation |

## Risk Rating Matrix

| Likelihood \ Impact | Low | Medium | High |
|--------------------|-----|--------|------|
| **Likely** | Medium | High | Critical |
| **Possible** | Low | Medium | High |
| **Unlikely** | Low | Low | Medium |

## Output Template

Each threat in THREAT_MODEL_REPORT.md should contain:
- Threat ID (format: T-{module}-{stride}-{NNN})
- STRIDE category
- Target module and interface
- Description of attack scenario
- Risk rating (Critical/High/Medium/Low)
- Mitigation strategy
- Verification method (test case or control)
```

- [ ] **Step 2: Commit**

```bash
git add references/threat-modeling-guide.md
git commit -m "feat: add threat modeling reference guide"
```

---

### Task 5: Create `references/observability-architecture.md`

**Files:**
- Create: `references/observability-architecture.md`

- [ ] **Step 1: Create the file with observability patterns**

```markdown
# Observability Architecture Reference

Reference document for `extensions/observability-engineering/` and `devforge-ops-ready`.

## SLO/SLI Framework

### SLI Types

| SLI Category | Metric Example | Measurement |
|-------------|---------------|-------------|
| Availability | `good_requests / total_requests` | Over 30-day window |
| Latency | `histogram_quantile(0.99, latency_bucket)` | Per-request measurement |
| Error Rate | `error_requests / total_requests` | Per 5-minute window |
| Throughput | `requests_per_second` | Sustained load |

### SLO Target Guidelines

| Service Criticality | Availability | Latency (p99) | Error Rate |
|--------------------|-------------|---------------|-----------|
| Critical | 99.99% | 200ms | 0.01% |
| Standard | 99.9% | 500ms | 0.1% |
| Internal | 99% | 2000ms | 1% |

### Burn Rate Alerting

| Burn Rate Multiplier | Window | Action |
|---------------------|--------|--------|
| 2x | 1 hour | Page immediately |
| 1x | 6 hours | Create ticket |
| 0.5x | 3 days | Weekly review |

## Structured Logging Schema

Required fields for every log entry:
- `timestamp`: ISO-8601 with timezone
- `level`: DEBUG / INFO / WARN / ERROR / FATAL
- `service`: Module identifier
- `trace_id`: Distributed trace correlation ID
- `message`: Human-readable message
- `context`: Key-value pairs of relevant context

## Alert Severity Levels

| Level | Response Time | Channel | Example |
|-------|--------------|---------|---------|
| P1 | 15 minutes | Page + SMS | Service down |
| P2 | 1 hour | Slack/email | Elevated error rate |
| P3 | 1 business day | Ticket | Performance degradation |
```

- [ ] **Step 2: Commit**

```bash
git add references/observability-architecture.md
git commit -m "feat: add observability architecture reference"
```

---

### Task 6: Create `references/data-quality-framework.md`

**Files:**
- Create: `references/data-quality-framework.md`

- [ ] **Step 1: Create the file with data quality patterns**

```markdown
# Data Quality Framework Reference

Reference document for `devforge-data-pipeline` skill.

## Data Quality Rule Types

| Rule Type | Description | Example |
|-----------|-------------|---------|
| `row_count_delta` | Row count change within threshold | New run has > 90% of previous rows |
| `null_rate` | NULL percentage below threshold | `user_id` NULL rate < 1% |
| `freshness` | Data age within threshold | `created_at` within 1 hour |
| `uniqueness` | Field values are unique | `order_id` has no duplicates |
| `range_check` | Values within expected range | `age` between 0 and 150 |
| `pii_scan` | Detect and mask PII fields | Hash `email` and `phone` |
| `schema_drift` | Schema matches expected structure | No unexpected columns added |

## Dimensional Modeling Patterns

### Star Schema
- One central fact table surrounded by dimension tables
- Fact table: measures + foreign keys to dimensions
- Dimension tables: descriptive attributes

### Fact Table Types
| Type | Grain | Example |
|------|-------|---------|
| Transaction | One row per transaction | `fact_orders` |
| Periodic Snapshot | One row per period | `fact_daily_inventory` |
| Accumulating Snapshot | One row per process | `fact_order_fulfillment` |

## OLAP Database-Specific Features

| Database | Partition Strategy | Clustering | Best For |
|----------|-------------------|------------|----------|
| ClickHouse | MergeTree by date | ORDER BY tuple | Real-time analytics |
| Snowflake | Automatic micro-partitions | Clustering keys | Data warehouse |
| BigQuery | Partition by date/integer | Clustering columns | Petabyte scale |
```

- [ ] **Step 2: Commit**

```bash
git add references/data-quality-framework.md
git commit -m "feat: add data quality framework reference"
```

---

### Task 7: Update `skill/tools/precondition-checker.md`

**Files:**
- Modify: `skill/tools/precondition-checker.md`

**Context:** Need to add new phases for threat-modeling and data-pipeline skills.

- [ ] **Step 1: Read current phase map to understand structure**

Run: `head -n 50 skill/tools/precondition-checker.md`

- [ ] **Step 2: Add new phases to the phase transition map**

Add to the phase list:
```markdown
- `threat_modeling_completed` — after `devforge-threat-modeling` finishes
- `data_pipeline_completed` — after `devforge-data-pipeline` finishes
```

- [ ] **Step 3: Update acceptable phases for relevant skills**

Locate the phase map table and add entries for:
- `devforge-threat-modeling`: acceptable phases include `design_review_completed`, `architecture_validated`
- `devforge-data-pipeline`: acceptable phases include `architecture_design_completed`, `scaffolding_completed`

- [ ] **Step 4: Commit**

```bash
git add skill/tools/precondition-checker.md
git commit -m "feat: add threat_modeling and data_pipeline phases to precondition checker"
```

---

### Task 8: Update `skill/tools/intervention-checkpoint.md`

**Files:**
- Modify: `skill/tools/intervention-checkpoint.md`

**Context:** Add new human gate commands introduced in v1.4.

- [ ] **Step 1: Add new commands to the canonical command spec**

Add to the command list:
```markdown
**v1.4 new commands**:
- `[THREAT_MODEL]` — Trigger devforge-threat-modeling skill
- `[DATA_PIPELINE]` — Trigger devforge-data-pipeline skill
- `[SLO]` — Trigger observability-engineering extension
- `[PERFORMANCE_TEST]` — Trigger performance-testing extension
- `[FRONTEND_DESIGN]` — Trigger frontend-ui-system-design extension
```

- [ ] **Step 2: Commit**

```bash
git add skill/tools/intervention-checkpoint.md
git commit -m "feat: add v1.4 human gate commands"
```

---

### Task 9: Update `skill/tools/artifact-manager.md`

**Files:**
- Modify: `skill/tools/artifact-manager.md`

**Context:** Add new artifact types that v1.4 skills generate.

- [ ] **Step 1: Add new artifact types to the strategy table**

Add entries:
```markdown
| `security.xml` | System-level security specification | CRUD | `devforge-architecture-design` |
| `THREAT_MODEL_REPORT.md` | STRIDE threat analysis report | Append | `devforge-threat-modeling` |
| `dataflow.xml` | Data pipeline topology | CRUD | `devforge-data-pipeline` |
| `schema-olap.sql` | OLAP dimensional model DDL | CRUD | `devforge-data-pipeline` |
| `observability/slo-definitions.yaml` | SLO/SLI definitions | CRUD | `extensions/observability-engineering` |
| `observability/alerting-rules.yaml` | Alerting rules | CRUD | `extensions/observability-engineering` |
| `performance/baseline-config.yaml` | Performance baseline | CRUD | `extensions/performance-testing` |
```

- [ ] **Step 2: Commit**

```bash
git add skill/tools/artifact-manager.md
git commit -m "feat: add v1.4 artifact types"
```

---

### Task 10: Update `devforge-state.md` Template

**Files:**
- Modify: `devforge-state.md`

**Context:** Add new sections for characteristic tags, loaded extensions, security posture, and data pipeline status.

- [ ] **Step 1: Add `Characteristic Tags` section**

Insert after the `Artifact Index` section:
```markdown
## Characteristic Tags

Automatically extracted from PRD during requirement-analysis.
Format: `[tag_name]`

- [ ] `[microservice]`
- [ ] `[data-pipeline]`
- [ ] `[high-security]`
- [ ] `[ui-heavy]`
- [ ] `[performance-critical]`
- [ ] `[high-observability]`
```

- [ ] **Step 2: Add `Loaded Extensions` section**

```markdown
## Loaded Extensions

Track which extensions have been loaded and when.
Format: `extension_name @ YYYY-MM-DD`

- [ ] `ai-agent-design @ `
- [ ] `frontend-ui-system-design @ `
- [ ] `observability-engineering @ `
- [ ] `performance-testing @ `
```

- [ ] **Step 3: Add `Security Posture` section**

```markdown
## Security Posture

- Threat Modeling Status: `not_started` / `in_progress` / `completed`
- Security Audit Status: `not_started` / `passed` / `failed`
- Last Security Review Date: 
```

- [ ] **Step 4: Add `Data Pipeline Status` section**

```markdown
## Data Pipeline Status

- Data Flow Modeling: `not_started` / `in_progress` / `completed`
- ETL DAG Generation: `not_started` / `generated` / `validated`
- Data Quality Rules: `not_started` / `defined` / `verified`
```

- [ ] **Step 5: Commit**

```bash
git add devforge-state.md
git commit -m "feat: add v1.4 sections to STATE.md template"
```

---

## Validation

- [ ] **Step: Run packaging validation**

```bash
python scripts/package-plugin.py --mode all --output ./dist
```

Expected: All skills validate successfully (frontmatter check passes).

---

## Plan Self-Review

**Spec coverage check:**
- XML Schema Security extensions (Authorization, ThreatModel, KeyManagement, Audit) → Tasks 1 ✓
- XML Schema OLAP support (modelType, partitionKey, clusteringKey) → Task 2 ✓
- XML Schema FrontendProfile → Task 3 ✓
- Threat modeling reference → Task 4 ✓
- Observability reference → Task 5 ✓
- Data quality reference → Task 6 ✓
- Precondition checker phase updates → Task 7 ✓
- Intervention checkpoint commands → Task 8 ✓
- Artifact manager new types → Task 9 ✓
- STATE.md template updates → Task 10 ✓

**Placeholder scan:** No TBD/TODO/fill-in-details found. ✓

**Type consistency:** All XML node names and attribute names are consistent between schema spec and skill references. ✓
