# DevForge v1.4 Integration Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Update installer scripts, plugin metadata, and perform final validation for v1.4 release.

**Architecture:** All v1.4 changes are integrated. This plan updates distribution artifacts and validates the complete package.

**Tech Stack:** Shell, PowerShell, JSON, Markdown

---

## File Structure

| File | Action | Responsibility |
|------|--------|---------------|
| `install.sh` | Modify | Add new core skills to `core_skills` array |
| `install.ps1` | Modify | Add new core skills to `$coreSkills` array |
| `uninstall.sh` | Modify | Add new core skills to `skills` array |
| `uninstall.ps1` | Modify | Add new core skills to `$skills` array |
| `.claude-plugin/plugin.json` | Modify | Update version to 1.4.0 |
| `.claude-plugin/marketplace.json` | Modify | Update version and description |

---

### Task 1: Update Installer Scripts

**Files:**
- Modify: `install.sh`, `install.ps1`, `uninstall.sh`, `uninstall.ps1`

**Required modifications:**

In all four scripts, add `devforge-threat-modeling` and `devforge-data-pipeline` to the skill arrays.

**`install.sh` (line 132):** Add after `devforge-security-audit`:
```bash
        devforge-threat-modeling
        devforge-data-pipeline
```

**`install.ps1` (line 126):** Add after `"devforge-security-audit"`:
```powershell
        "devforge-threat-modeling"
        "devforge-data-pipeline"
```

**`uninstall.sh` (line 29):** Add after `devforge-security-audit`:
```bash
    devforge-threat-modeling
    devforge-data-pipeline
```

**`uninstall.ps1` (line 24):** Add after `"devforge-security-audit"`:
```powershell
    "devforge-threat-modeling"
    "devforge-data-pipeline"
```

---

### Task 2: Update Plugin Metadata

**Files:**
- Modify: `.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json`

**Required modifications:**

**`plugin.json`:**
- Change `"version": "1.3.0"` to `"version": "1.4.0"`
- Update description to mention v1.4 features:
  ```json
  "description": "DevForge Chain for Claude Code: 12 skills covering the full software development lifecycle from requirements to production deployment, built on the VCMF framework and DIVE cycle. v1.4 adds threat modeling, data pipeline design, microservices infrastructure, and 3 domain extensions."
  ```

**`marketplace.json`:**
- Change `"version": "1.3.0"` to `"version": "1.4.0"`
- Update description to mention v1.4 features:
  ```json
  "description": "DevForge Chain v1.4: 12 core skills + 6 domain extensions covering full SDLC. New in v1.4: threat modeling (STRIDE), data pipeline (ETL DAG + OLAP), microservices (Service Mesh + Saga), frontend UI design system, observability engineering (SLO/SLI), and performance testing. Built on VCMF framework with DIVE cycle."
  ```

---

### Task 3: Final Validation

- [ ] **Run packaging validation**

```bash
python scripts/package-plugin.py --mode all --output ./dist
```

Expected: All 19 skills validate successfully.

- [ ] **Verify installer script syntax**

```bash
bash -n install.sh
```

Expected: No syntax errors.

---

## Plan Self-Review

**Spec coverage check:**
- Installer scripts updated → Task 1 ✓
- Plugin metadata updated → Task 2 ✓
- Final validation → Task 3 ✓

**Placeholder scan:** No TBD/TODO found. ✓
